import{fireEvent,render,screen}from'@testing-library/react';import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{Phase7App}from'./Phase7App';beforeEach(()=>{globalThis.fetch=vi.fn().mockImplementation((url:string)=>Promise.resolve({ok:true,json:async()=>url.includes('scanner-analytics')?{total:0,completed:0,dataMissing:0,calculationVersion:'performance-v2',targetRate:3,stopRate:-2,targetStop:{sampleSize:0,targetFirst:0,stopFirst:0,neither:0,targetFirstRate:null,stopFirstRate:null,expectancy:null},timeBuckets:[],signalCombinations:[],historicalEdge:{sampleSize:0,winRate:null,averageReturn:null,expectancy:null,averageMfe:null,averageMae:null,confidence:'LOW',enoughSamples:false},minimumSampleSize:20}:[] }))as typeof fetch});test('opens scanner analytics',async()=>{render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><Phase7App/></QueryClientProvider>);fireEvent.click(screen.getByRole('button',{name:/성과 분석/}));expect(screen.getByRole('heading',{name:'신호 성과 분석'})).toBeInTheDocument();expect(screen.getByText('사용 안내')).toBeInTheDocument();expect(await screen.findByText('분석할 탐지 성과가 없습니다')).toBeInTheDocument()});
