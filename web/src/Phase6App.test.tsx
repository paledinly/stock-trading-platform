import{fireEvent,render,screen}from'@testing-library/react';import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{Phase6App}from'./Phase6App';beforeEach(()=>{globalThis.fetch=vi.fn().mockResolvedValue({ok:true,json:async()=>[]})as typeof fetch});test('opens market radar workspace',async()=>{render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><Phase6App/></QueryClientProvider>);fireEvent.click(screen.getByRole('button',{name:/실시간 레이더/}));expect(screen.getByRole('heading',{name:'실시간 레이더'})).toBeInTheDocument();expect(screen.getByText('사용 안내')).toBeInTheDocument();expect(await screen.findByText('포착된 후보가 없습니다')).toBeInTheDocument()});
