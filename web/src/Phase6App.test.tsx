import{fireEvent,render,screen}from'@testing-library/react';import{QueryClient,QueryClientProvider}from'@tanstack/react-query';import{Phase6App}from'./Phase6App';beforeEach(()=>{globalThis.fetch=vi.fn().mockResolvedValue({ok:true,json:async()=>[]})as typeof fetch});test('opens scanner workspace',async()=>{render(<QueryClientProvider client={new QueryClient({defaultOptions:{queries:{retry:false}}})}><Phase6App/></QueryClientProvider>);fireEvent.click(screen.getByRole('button',{name:/Scanner/}));expect(screen.getByRole('heading',{name:'실시간 Scanner'})).toBeInTheDocument();expect(await screen.findByText('아직 포착된 종목이 없습니다')).toBeInTheDocument()});
