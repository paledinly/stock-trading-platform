# Database

PostgreSQL is the system of record. Flyway V2 adds the `stock` table with unique stock code, market,
product classification, management/trading-halt flags and master synchronization timestamp.
